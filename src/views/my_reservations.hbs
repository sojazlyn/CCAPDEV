<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <title>My Reservations</title>
</head>
<body class="bg-light">
  <section id="my" class="container my-5">
    <h3 class="mb-4">My Reservations</h3>
    <div id="bookingsList" class="list-group shadow-sm">
      {{#each reservations}}
      <div class="list-group-item list-group-item-action d-flex justify-content-between align-items-start">
        <div>
          <div class="fw-bold">{{this.flightId.flightNumber}} ({{this.bookingStatus}})</div>
          <div>{{this.flightId.origin}} → {{this.flightId.destination}}</div>
          <div>Passenger: {{this.passengerName}}</div>
          <div>Seat: {{this.selectedSeat}}</div>
        </div>
        <div class="d-flex flex-column">
          <span class="fw-bold mb-2">PHP {{this.totalCost}}</span>
          {{#if (eq this.bookingStatus "Confirmed")}}
          <form action="/reservations/cancel/{{this._id}}" method="POST">
            <button type="submit" class="btn btn-sm btn-danger">Cancel</button>
          </form>
         {{#if (eq this.bookingStatus "Confirmed")}}
          <div class="btn-group">
            <form action="/reservations/cancel/{{this._id}}" method="POST" style="display: inline;">
              <button type="submit" class="btn btn-sm btn-danger">Cancel</button>
            </form>
            <a href="/reservations/edit/{{this._id}}" class="btn btn-sm btn-warning">Edit Packages</a>
          </div>
          {{/if}}
        </div>
      </div>
      {{else}}
      <p>You have no reservations.</p>
      {{/each}}
    </div>
  </section>
</body>
</html>
